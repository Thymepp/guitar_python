![Python Data Types](./pic/python_data_types.jpg)
# Python Data Types

## Numbers
- **Int** — `20` — จำนวนเต็ม (Integer)
- **Float** — `35.75` — จำนวนทศนิยม (Floating-point)
- **Complex** — `1+3j` — จำนวนเชิงซ้อน (Complex number)

## Bool
- **True** — ถูก / จริง ที่แปลว่า **ถูกต้องหรือเป็นจริง**
- **False** — ผิด / เท็จ ที่แปลว่า **ไม่ถูกต้องหรือไม่เป็นจริง**
- **Truthy values** — ค่าที่ Python มองว่าเป็น `True`
  - `1`
  - `"Hello"`
  - `[1, 2]`
  - `{1, 2}`
- **Falsy values** — ค่าที่ Python มองว่าเป็น `False`
  - `0`
  - `""`
  - `[]`
  - `{}`
  - `None`
  - `False`

## Set

- **Set** — `{2, 4, 6}` — เซตข้อมูลที่ **ไม่ซ้ำกัน** และ **ไม่มีลำดับที่แน่นอน**

## Dict

- **Dictionary (Dict)** — `{1: 'a', 2: 'b'}` — ข้อมูลแบบ **Key-Value**

## Sequence

- **String** — `'Jessa'` — ข้อความหรือลำดับของตัวอักษร
- **List** — `[2, 'a', 5.7]` — รายการข้อมูลที่ **แก้ไขได้ (Mutable)**
- **Tuple** — `(3, 4.5, 'b')` — รายการข้อมูลที่ **แก้ไขไม่ได้ (Immutable)**
